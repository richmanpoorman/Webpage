import type { ReactElement } from "react";
// TODO :: Refactor the navigation bar

type NavigationBarProps = {
    className? : string
};

function NavigationBar({className = ""} : NavigationBarProps) : ReactElement {

    return (
        <div></div>
    );
}